# Legacy Image Build Pipeline

This folder contains the previous custom image build workflow based on pi-gen.

Archived content includes:
- pi-gen source and custom stages
- Docker/native image build scripts
- quick image patching scripts
- old build configuration

The active project path now uses stock Raspberry Pi OS with post-install bootstrap.
See the repository root README for current usage.